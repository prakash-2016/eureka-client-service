spring:
  application:
    name: stock-service

server:
  port: 8301


eureka:
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://127.0.0.1:8302/eureka/
  instance:
    hostname: 127.0.0.1